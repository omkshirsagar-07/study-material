// # JavaScript Practice Questions

// ## 🔹 for...of Loop (10 Questions)
// 1. Use `for...of` to print all elements of an array `[1, 2, 3, 4]`.
// 2. Use `for...of` to print each character of the string `"hello"`.
// 3. Use `for...of` to print all numbers in `[10, 20, 30]`.
// 4. Use `for...of` to find the sum of numbers in `[5, 10, 15]`.
// 5. Use `for...of` to print all colors in `["red", "green", "blue"]`.
// 6. Use `for...of` to print the square of each number in `[2, 4, 6]`.
// 7. Use `for...of` to print names in `["Alice", "Bob", "Charlie"]`.
// 8. Use `for...of` to print each element of `[true, false, true]`.
// 9. Use `for...of` to count how many elements are in `[1, 2, 3, 4, 5]`.
// 10. Use `for...of` to print the doubled value of each number in `[1, 3, 5]`.

// ---

// ## 🔹 for...in Loop (10 Questions)
// 1. Use `for...in` to print all keys of `{a: 1, b: 2, c: 3}`.
// 2. Use `for...in` to print all values of `{x: 10, y: 20}`.
// 3. Use `for...in` to print both keys and values of `{name: "John", age: 25}`.
// 4. Use `for...in` to count the number of keys in `{p: 5, q: 10, r: 15}`.
// 5. Use `for...in` to print each property of `{fruit: "apple", color: "red"}`.
// 6. Use `for...in` to create a string of keys from `{a: 1, b: 2}`.
// 7. Use `for...in` to check if the key `"age"` exists in `{name: "Sam", age: 30}`.
// 8. Use `for...in` to print values greater than 10 in `{a: 5, b: 15, c: 25}`.
// 9. Use `for...in` to print the type of each value in `{name: "Eva", age: 22}`.
// 10. Use `for...in` to copy all keys into an array from `{id: 1, code: 2}`.

// ---

// ## 🔹 filter (10 Questions)
// 1. Use `filter` to get even numbers from `[1, 2, 3, 4, 5, 6]`.
// 2. Use `filter` to get odd numbers from `[10, 11, 12, 13]`.
// 3. Use `filter` to get numbers greater than 5 from `[2, 8, 3, 10]`.
// 4. Use `filter` to get words with length greater than 3 from `["hi", "hello", "to", "world"]`.
// 5. Use `filter` to get numbers less than 10 from `[5, 15, 8, 20]`.
// 6. Use `filter` to get strings starting with `"a"` from `["apple", "banana", "avocado"]`.
// 7. Use `filter` to get positive numbers from `[-3, -1, 0, 2, 4]`.
// 8. Use `filter` to get values equal to `"cat"` from `["dog", "cat", "lion", "cat"]`.
// 9. Use `filter` to get numbers divisible by 3 from `[3, 6, 7, 9, 10]`.
// 10. Use `filter` to get names with length exactly 4 from `["John", "Paul", "Sam"]`.

// ---

// ## 🔹 map (10 Questions)
// 1. Use `map` to double each number in `[1, 2, 3]`.
// 2. Use `map` to square each number in `[2, 3, 4]`.
// 3. Use `map` to convert each string in `["a", "b", "c"]` to uppercase.
// 4. Use `map` to get the length of each word in `["hi", "hello", "world"]`.
// 5. Use `map` to add 10 to each number in `[5, 10, 15]`.
// 6. Use `map` to change numbers into strings from `[1, 2, 3]`.
// 7. Use `map` to get the first character of each word in `["cat", "dog", "lion"]`.
// 8. Use `map` to make each number negative from `[1, 2, 3]`.
// 9. Use `map` to create an array of `true`/`false` if numbers are even in `[2, 5, 8]`.
// 10. Use `map` to append `"!"` to each word in `["hi", "bye"]`.
